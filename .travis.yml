matrix:
  include:
    - language: rust
      rust:
        - nightly
      script:
        - cargo test -- --test-threads 1
      services:
        - redis-server
    - language: node_js
      node_js:
        - 13.8
      install:
        - npm install
      script: bash ./travis.sh
